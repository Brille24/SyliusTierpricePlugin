{% set product = hookable_metadata.context.resource %}

{% if not product.isConfigurable() %}
    <button
        type="button"
        class="list-group-item list-group-item-action {% if hookable_metadata.configuration.active|default(false) %}active{% endif %}"
        data-bs-toggle="tab"
        data-bs-target="#product-tierprice"
        role="tab"
        {{ sylius_test_html_attribute('side-navigation-tab', 'tierprices') }}
    >
        {{ 'brille24_tier_price.ui.tier_prices'|trans }}
    </button>
{% endif %}
